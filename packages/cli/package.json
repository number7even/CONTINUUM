{
  "name": "@number7even/continuum-cli",
  "version": "0.0.1",
  "description": "Continuum CLI — continuum init / start / serve / status / import-state for new operators.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "continuum": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/index.js",
    "test": "node --test",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@number7even/continuum-core": "0.0.1",
    "@number7even/continuum-mcp-server": "0.0.1"
  },
  "license": "Apache-2.0",
  "author": "Riaan Kleynhans <riaan@number7even.com>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/number7even/CONTINUUM.git",
    "directory": "packages/cli"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  }
}
