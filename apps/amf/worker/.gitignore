# Python venv + model caches (never commit — multi-GB)
.venv/
__pycache__/
*.pyc
# worker run artifacts (audio, renders)
out/
render-out/
*.wav
*.mp4
node_modules/

.env.local

# operator fuel — voice recordings, b-roll, scripts (P1: never commit)
fuel/
